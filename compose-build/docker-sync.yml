version: "2"
options:
  verbose: false
syncs:
  build-sync:
    #notify_terminal: true
    src: ../../build/
    sync_host_ip: 'auto'
  source-sync:
    #notify_terminal: true
    src: ../../src/
    sync_host_ip: 'auto'
  dependency-sync:
    #notify_terminal: true
    src: ../../dependencies/
    sync_host_ip: 'auto'
  depsbuild-sync:
    #notify_terminal: true
    src: ../../deps_build/
    sync_host_ip: 'auto'
  ide-sync:
    #notify_terminal: true
    src: ../../ide_build/
    sync_host_ip: 'auto'

